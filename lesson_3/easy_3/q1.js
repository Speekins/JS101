//Write three different ways to remove all of the elements from the following array:

let numbers = [1, 2, 3, 4];

//solution 1: numbers = [];

/*solution 2: while (numbers[0]) {
  numbers.pop();
}*/

//solution 3: numbers.splice(0);

console.log(numbers);